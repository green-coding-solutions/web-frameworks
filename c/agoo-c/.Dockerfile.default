FROM debian:bullseye

WORKDIR /usr/src/app

RUN apt-get -qq update
RUN apt-get -qy install build-essential wget


  COPY 'Makefile' 'Makefile'
  COPY 'simple.c' 'simple.c'

  RUN wget -c https://github.com/ohler55/agoo-c/tarball/v0.7.3 -O agoo.tar.gz
  RUN tar xzf agoo.tar.gz
  RUN mv ohler55* agoo-c
  RUN cd agoo-c/src && make

   RUN make

FROM debian:bullseye

WORKDIR /opt/web

RUN apt-get -qq update


   COPY --from=0 /usr/src/app/simple simple



CMD /opt/web/simple
