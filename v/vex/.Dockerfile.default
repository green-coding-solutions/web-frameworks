FROM thevlang/vlang:debian as build

WORKDIR /app

  COPY 'main.v' 'main.v'

  RUN v install nedpals.vex

RUN v . -prod -o server

FROM alpine

WORKDIR /app

COPY --from=build /app /app

CMD /app/server
